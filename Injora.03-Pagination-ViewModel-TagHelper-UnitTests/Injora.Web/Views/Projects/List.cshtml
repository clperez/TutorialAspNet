@model PagedListViewModel<Project>

@foreach (var p in Model.Entities)
{
    <div>
        <h3>@p.Name</h3>
        @p.Description
        <h4>@p.CampaignMinimum.ToString("c")</h4>
    </div>
}


<div page-model="@Model.PagingInfo" page-action="List"></div>